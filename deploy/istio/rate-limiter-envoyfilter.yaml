apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: rate-limiter-wasm
  namespace: istio-system
spec:
  workloadSelector:
    labels:
      app: istio-ingressgateway
  configPatches:
    - applyTo: CLUSTER
      match:
        context: GATEWAY
      patch:
        operation: ADD
        value:
          name: counter-service
          type: STRICT_DNS
          connect_timeout: 1s
          load_assignment:
            cluster_name: counter-service
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: counter-service.default.svc.cluster.local
                          port_value: 8080
    - applyTo: HTTP_FILTER
      match:
        context: GATEWAY
        listener:
          filterChain:
            filter:
              name: envoy.filters.network.http_connection_manager
              subFilter:
                name: envoy.filters.http.router
      patch:
        operation: INSERT_BEFORE
        value:
          name: envoy.filters.http.wasm
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.http.wasm.v3.Wasm
            config:
              name: rate-limiter-wasm
              root_id: rate-limiter-root
              vm_config:
                vm_id: rate-limiter-vm
                runtime: envoy.wasm.runtime.v8
                code:
                  remote:
                    http_uri:
                      uri: http://counter-service.default.svc.cluster.local:8080/wasm/rate-limiter.wasm
                      cluster: counter-service
                      timeout: 10s
                    sha256: bcbbe8b98a496ea040832a0ed4888c63cdb7eb10470fa7a0c767d448e78e7326
              configuration:
                "@type": type.googleapis.com/google.protobuf.StringValue
                value: |
                  domains:
                    - llm-demo.local
                  token_statistics:
                    enabled: true
                    inject_stream_usage: true
                    metric_key_limit: 5000
                  distributed_limit:
                    enabled: true
                    backend: counter_service
                    counter_service:
                      cluster: counter-service
                      acquire_path: /acquire
                      release_path: /release
                      timeout_ms: 5000
                      lease_ttl_ms: 30000
                  error_response:
                    status_code: 429
                    message: Rate limit exceeded
